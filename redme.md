1️⃣ What is the difference between var, let, and const?

1. var is an old method so we don't use it
2. If we use const that variable cannot be changed anymore
3. and if we use let it can be changed multiple times

2️⃣ What is the spread operator (...)?

   The spread operator is a feature of JavaScript ES6, and it is represented by three dots (...).


3️⃣ What is the difference between map(), filter(), and forEach()?
   
1. map() works on every element of an array and creates a new array
2. filter() checks a condition and creates a new array based on that
3. forEach() just runs a loop for every element It doesn't create a new array

4️⃣ What is an arrow function?

   Arrow function is a method in JavaScript ES6 It is used to write short functions in a single line

5️⃣ What are template literals?

   Template literals are something that makes everything inside them dynamic and easy to use